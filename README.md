# Elementary Cellular Automaton
A C program that simulates how a row of cells evolves over generations using user-selected rules.

## How it works

The program takes a rule number from 0 to 255 and uses its binary bits to determine how each cell changes based on its neighbors.

It uses arrays, functions, bitwise operations, and loops to calculate and display each generation, while tracking the cumulative number of active cells.
